server:
  port: 8888

spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vietluannguyen711/config_server.git
          clone-on-start: true