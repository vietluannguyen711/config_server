eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  datasource:
    url: jdbc:postgresql://course_db:5432/coursedb
    driverClassName: org.postgresql.Driver
    username: root
    password: Naul126@.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8082